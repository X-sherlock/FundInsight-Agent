services:
  - type: web
    name: fundinsight-agent-api
    runtime: python
    plan: free
    region: singapore
    branch: main
    autoDeploy: true
    buildCommand: pip install -e .
    startCommand: uvicorn fundinsight.api:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
    envVars:
      - key: FUNDINSIGHT_ENFORCE_REPORT_GUARD
        value: "false"
      - key: FUNDINSIGHT_CORS_ALLOW_ORIGINS
        fromService:
          name: fundinsight-agent-web
          type: web
          property: host
      - key: DASHSCOPE_API_KEY
        sync: false
      - key: DASHSCOPE_MODEL
        value: qwen-plus
      - key: DASHSCOPE_BASE_URL
        value: https://dashscope.aliyuncs.com/compatible-mode/v1
      - key: FUNDINSIGHT_LLM_PROVIDER
        value: bailian

  - type: web
    name: fundinsight-agent-web
    runtime: static
    plan: free
    region: singapore
    branch: main
    autoDeploy: true
    buildCommand: cd frontend && npm ci && npm run build
    staticPublishPath: frontend/dist
    envVars:
      - key: VITE_API_BASE_URL
        fromService:
          name: fundinsight-agent-api
          type: web
          property: host
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
